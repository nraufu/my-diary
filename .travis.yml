language: node_js
node_js:
 - "10.16.3"
cache: npm

install: 
  -npm install

script:
 -npm run test

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on: 
    branch: develop